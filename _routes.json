{
  "version": 1,
  "description": "Invoke Pages Functions for the API and for '/' (functions/_middleware.js rewrites subdomain roots to their pages); everything else is served as a static asset.",
  "include": ["/api/*", "/"],
  "exclude": []
}
